@model IEnumerable<Editorial>

@{
    ViewData["Title"] = "Editoriales";
}

<h1>Editoriales</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Crear editorial</a>
</p>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Descripcion</th>
            <th>Activo</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model)
{
        <tr>
            <td>@item.Nombre</td>
            <td>@item.Descripcion</td>
            <td>@(item.Activo ? "Si" : "No")</td>
            <td class="text-end">
                <a asp-action="Edit" asp-route-id="@item.IdEditorial">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdEditorial">Detalle</a> |
                <a asp-action="Delete" asp-route-id="@item.IdEditorial">Baja</a>
            </td>
        </tr>
}
    </tbody>
</table>
